package Behavioral_DP.src.formatters;

//Implement the same thing that is formating text but there could be multiple ways or you can say strategy therfore
//strategy design pattern
public interface TextFormattingStrategy {

    public String format(String text);
}
